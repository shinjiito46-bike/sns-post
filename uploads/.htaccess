# uploadsディレクトリの保護
# PHPの実行を無効化

# PHPファイルへのアクセスを拒否
<FilesMatch "\.ph(p[345]?|t|tml)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>
