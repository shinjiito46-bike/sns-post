# SNS同時投稿システム - Lolipop対応設定

# デフォルトファイル設定
DirectoryIndex index.php index.html

# セキュリティヘッダー
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
</IfModule>

# 機密ファイルへのアクセス拒否（Apache 2.2/2.4両対応）
<FilesMatch "\.(sql|log|env)$">
    Order allow,deny
    Deny from all
</FilesMatch>

<Files ".env">
    Order allow,deny
    Deny from all
</Files>
